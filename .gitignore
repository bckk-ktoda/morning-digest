# Claude project settings (personal overrides)
.claude/settings.local.json

# Runtime state (ephemeral per-run data)
state/daily_context.json
state/run_status.json

# Archive stays local
state/archive/

# Keep agents, commands, settings tracked
!.claude/
!.claude/agents/
!.claude/agents/**
!.claude/commands/
!.claude/commands/**
!.claude/settings.json
